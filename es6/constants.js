'use strict';

const CTRL_C = '\u0003',
      LINE_FEED = '\n',
      CARRIAGE_RETURN = '\r',
      BACKSPACE = String.fromCharCode(127);

const constants = {
  CTRL_C: CTRL_C,
  LINE_FEED: LINE_FEED,
  CARRIAGE_RETURN: CARRIAGE_RETURN,
  BACKSPACE: BACKSPACE
};

module.exports = constants;
