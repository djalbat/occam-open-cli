"use strict";

const { HELP_OPTION, VERSION_OPTION, LOG_LEVEL_OPTION } = require("./options");

const h = HELP_OPTION,
      v = VERSION_OPTION,
      l = LOG_LEVEL_OPTION

module.exports = {
  h,
  v,
  l
};
